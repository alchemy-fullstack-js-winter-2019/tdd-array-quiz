
describe('removeWords', () => {
  it('will remove any word with the letter Y or y', () => {
    const words = ['sky', 'hot', 'Yeti', 'green'].includes(ele => {
      return ele === [1, 3];
    });
    expect(words);
  });
});
